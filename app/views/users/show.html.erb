<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Image:</strong>
  <td><%= image_tag image_base64(@user.image) %></td>
</p>

<%= link_to 'Edit', edit_user_path(id: @user.id) %> |
<%= link_to 'Back', users_path %>

